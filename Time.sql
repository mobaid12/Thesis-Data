CREATE TABLE `IVR_Time` (
  `Transaction_id` int AUTOINCREMENT,
  `Star_Time` varchar(50),
  `End_Time` varchar(50)
);

CREATE TABLE `MOP_Time` (
  `Transaction_id` int AUTOINCREMENT,
  `Star_Time` varchar(50),
  `End_Time` varchar(50)
);

CREATE TABLE `Bank_Time` (
  `Transaction_id` int AUTOINCREMENT,
  `Star_Time` varchar(50),
  `End_Time` varchar(50)
);

CREATE TABLE `CentralBank_Time` (
  `Transaction_id` int AUTOINCREMENT,
  `Star_Time` varchar(50),
  `End_Time` varchar(50)
);
